spring.datasource.url=${DATABASE_URL:jdbc:postgresql://postgres.railway.internal:5432/railway}
spring.datasource.username=${PGUSER:postgres}
spring.datasource.password=${PGPASSWORD:oNSxBLxQWKcpqhEKEHcMAMccWAtMcbaG}
spring.jpa.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=${PORT:8080}
spring.datasource.hikari.connection-timeout=30000    # 30 seconds
spring.datasource.hikari.maximum-pool-size=10       # Increase pool size
spring.datasource.hikari.minimum-idle=5             # Keep some connections ready
spring.datasource.hikari.idle-timeout=300000        # 5 minutes
spring.datasource.hikari.max-lifetime=1800000       # 30 minutes
spring.datasource.hikari.connection-test-query=SELECT 1  # Test connection
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=true
